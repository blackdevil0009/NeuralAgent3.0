version: '3.8'

services:
  iris-core:
    build: .
    container_name: iris-neural-core
    ports:
      - "6754:6754"
    environment:
      - GOOGLE_API_KEY=${GEMINI_API_KEY}
      - IRIS_VOICE=Lyra
    restart: unless-stopped
